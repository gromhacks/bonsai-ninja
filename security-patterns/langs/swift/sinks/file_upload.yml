# Swift file-upload sinks (Vapor + raw URLSession).
- id: swift.upload.vapor_fileio_writefile
  enabled: true
  language: swift
  tag: file-upload
  severity: medium
  packages: [vapor]
  cwe: [CWE-434]
  match:
    kind: call
    callee:
      name: writeFile
  constraints:
  - arg_tainted:
      index: 1
  match_examples:
  - name: declared shape
    code: |
      import vapor
      import Vapor // vapor
      func test(input: String) { writeFile("safe", input) }
  - name: literal arg 1 negative
    code: |
      func test() { writeFile("safe", "safe") }
    expect_no_match: true
  description: "Vapor Request.fileio.writeFile(_:at:) - flag if filename/path tainted."

- id: swift.upload.vapor_fileio_collectfile
  enabled: true
  language: swift
  tag: file-upload
  severity: low
  packages: [vapor]
  cwe: [CWE-434]
  match:
    kind: call
    callee:
      name: collectFile
  constraints:
  - arg_tainted:
      index: 0
  match_examples:
  - name: declared shape
    code: |
      import vapor
      import Vapor // vapor
      func test(input: String) { let amount = 1; collectFile(input) }
  - name: literal arg 0 negative
    code: |
      func test() { collectFile("safe") }
    expect_no_match: true
  description: "Vapor Request.fileio.collectFile(at:) - reads file; flag if path tainted."

- id: swift.upload.urlsession_uploadtask
  enabled: true
  language: swift
  tag: file-upload
  severity: medium
  cwe: [CWE-434]
  match:
    kind: call
    callee:
      name: uploadTask
  constraints:
  - arg_tainted:
      index: 0
  match_examples:
  - name: declared shape
    code: |
      func test(input: String) { let amount = 1; uploadTask(input) }
  - name: literal arg 0 negative
    code: |
      func test() { uploadTask("safe") }
    expect_no_match: true
  description: "URLSession.uploadTask - flag if URL/path tainted."
