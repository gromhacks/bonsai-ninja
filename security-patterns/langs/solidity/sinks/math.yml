- id: solidity.math.unchecked_block
  enabled: false
  disabled_reason:
    code: requires-constraint
  language: solidity
  tag: integer-overflow
  severity: medium
  cwe: [CWE-190]
  match:
    kind: call
    callee:
      name: unchecked
  description: "Disabled - `unchecked { ... }` disables 0.8+ overflow checks; re-enable once arithmetic-in-block analysis is wired up."
