- id: solidity.unchecked_return.call
  enabled: false
  disabled_reason:
    code: over-broad
  language: solidity
  tag: unchecked-return
  severity: medium
  cwe: [CWE-252]
  match:
    kind: call
    callee:
      name: call
  description: "Disabled — v1 matching sees only .call and cannot prove the bool return is unchecked; reentrancy.call covers the external-call sink shape."
- id: solidity.unchecked_return.send
  enabled: false
  disabled_reason:
    code: over-broad
  language: solidity
  tag: unchecked-return
  severity: medium
  cwe: [CWE-252]
  match:
    kind: call
    callee:
      name: send
  description: "Disabled — v1 matching sees only .send and cannot prove the bool return is unchecked; reentrancy.send covers the external-call sink shape."
