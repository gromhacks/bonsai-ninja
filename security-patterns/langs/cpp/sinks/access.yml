- id: cpp.access.aws_s3_get_object
  enabled: true
  language: cpp
  tag: access-control
  severity: high
  packages: [aws-sdk-cpp-s3]
  cwe: [CWE-284, CWE-639]
  match:
    kind: call
    callee:
      attribute: [S3Client, GetObject]
  constraints:
  - arg_tainted:
      index: 0
  match_examples:
  - name: generated direct match
    code: |
      #include <aws-sdk-cpp-s3.h>
      void example() {
        const char *arg = "tainted";
        const char *fmt = arg;
        const char *query = arg;
        S3Client::GetObject(arg);
      }
  - name: literal s3 get object negative
    code: |
      #include <aws-sdk-cpp-s3.h>
      void example() {
        S3Client::GetObject("reports/fixed.csv");
      }
    expect_no_match: true
  description: "AWS SDK C++ S3Client::GetObject - tainted bucket/key selector reads arbitrary object data."

- id: cpp.access.aws_s3_put_object
  enabled: true
  language: cpp
  tag: access-control
  severity: high
  packages: [aws-sdk-cpp-s3]
  cwe: [CWE-284, CWE-639]
  match:
    kind: call
    callee:
      attribute: [S3Client, PutObject]
  constraints:
  - arg_tainted:
      index: 0
  match_examples:
  - name: generated direct match
    code: |
      #include <aws-sdk-cpp-s3.h>
      void example() {
        const char *arg = "tainted";
        const char *fmt = arg;
        const char *query = arg;
        S3Client::PutObject(arg);
      }
  - name: literal s3 put object negative
    code: |
      #include <aws-sdk-cpp-s3.h>
      void example() {
        S3Client::PutObject("reports/fixed.csv");
      }
    expect_no_match: true
  description: "AWS SDK C++ S3Client::PutObject - tainted bucket/key selector overwrites attacker-chosen object keys."

- id: cpp.access.aws_secretsmanager_get_secret_value
  enabled: true
  language: cpp
  tag: access-control
  severity: high
  packages: [aws-sdk-cpp-secretsmanager]
  cwe: [CWE-284]
  match:
    kind: call
    callee:
      attribute: [SecretsManagerClient, GetSecretValue]
  constraints:
  - arg_tainted:
      index: 0
  match_examples:
  - name: generated direct match
    code: |
      #include <aws-sdk-cpp-secretsmanager.h>
      void example() {
        const char *arg = "tainted";
        const char *fmt = arg;
        const char *query = arg;
        SecretsManagerClient::GetSecretValue(arg);
      }
  - name: literal secretsmanager get secret value negative
    code: |
      #include <aws-sdk-cpp-secretsmanager.h>
      void example() {
        SecretsManagerClient::GetSecretValue("app/prod/db");
      }
    expect_no_match: true
  description: "AWS SDK C++ SecretsManagerClient::GetSecretValue - tainted secret id reads arbitrary secret material."

- id: cpp.access.azure_blob_download
  enabled: true
  language: cpp
  tag: access-control
  severity: high
  packages: [azure-storage-blobs-cpp]
  cwe: [CWE-284, CWE-639]
  match:
    kind: call
    callee:
      attribute: [BlobClient, DownloadTo]
  constraints:
  - arg_tainted:
      index: 0
  match_examples:
  - name: generated direct match
    code: |
      #include <azure-storage-blobs-cpp.h>
      void example() {
        const char *arg = "tainted";
        const char *fmt = arg;
        const char *query = arg;
        BlobClient::DownloadTo(arg);
      }
  - name: literal azure blob download negative
    code: |
      #include <azure-storage-blobs-cpp.h>
      void example() {
        BlobClient::DownloadTo("/var/app/export/report.csv");
      }
    expect_no_match: true
  description: "Azure BlobClient::DownloadTo - tainted blob selector reads arbitrary object data."

- id: cpp.access.azure_blob_upload
  enabled: true
  language: cpp
  tag: access-control
  severity: high
  packages: [azure-storage-blobs-cpp]
  cwe: [CWE-284, CWE-639]
  match:
    kind: call
    callee:
      attribute: [BlockBlobClient, UploadFrom]
  constraints:
  - arg_tainted:
      index: 0
  match_examples:
  - name: tainted azure blob upload source path
    code: |
      #include <azure-storage-blobs-cpp.h>
      void example() {
        const char *arg = "tainted";
        const char *fmt = arg;
        const char *query = arg;
        BlockBlobClient::UploadFrom(arg);
      }
  - name: literal azure blob upload path negative
    code: |
      void example() {
        BlockBlobClient::UploadFrom("/var/app/export/report.csv");
      }
    expect_no_match: true
  description: "Azure BlockBlobClient::UploadFrom(path) - tainted local file paths can upload arbitrary local files to cloud storage."

- id: cpp.access.gcp_secretmanager_access_secret_version
  enabled: true
  language: cpp
  tag: access-control
  severity: high
  packages: [google-cloud-cpp]
  cwe: [CWE-284]
  match:
    kind: call
    callee:
      attribute: [SecretManagerServiceClient, AccessSecretVersion]
  constraints:
  - arg_tainted:
      index: 0
  match_examples:
  - name: generated direct match
    code: |
      #include <google-cloud-cpp.h>
      void example() {
        const char *arg = "tainted";
        const char *fmt = arg;
        const char *query = arg;
        SecretManagerServiceClient::AccessSecretVersion(arg);
      }
  - name: literal gcp secret version negative
    code: |
      #include <google-cloud-cpp.h>
      void example() {
        SecretManagerServiceClient::AccessSecretVersion("projects/app/secrets/db/versions/latest");
      }
    expect_no_match: true
  description: "google-cloud-cpp SecretManagerServiceClient::AccessSecretVersion - tainted resource name reads arbitrary secret versions."
