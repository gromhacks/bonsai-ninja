- id: php.redos.preg_match
  enabled: false
  disabled_reason:
    code: requires-constraint
  language: php
  tag: redos
  severity: medium
  packages: [stdlib]
  cwe: [CWE-1333]
  match:
    kind: call
    callee:
      name: preg_match
  description: "Disabled - preg_match is usually called with tainted subject text; enable only when arg-specific taint can prove the pattern is attacker-controlled."

- id: php.redos.preg_replace
  enabled: false
  disabled_reason:
    code: over-broad
  language: php
  tag: redos
  severity: medium
  packages: [stdlib]
  cwe: [CWE-1333]
  match:
    kind: call
    callee:
      name: preg_replace
  description: "Disabled - preg_replace is noisy without arg-specific taint proving the regex pattern, not the replacement subject, is attacker-controlled."
