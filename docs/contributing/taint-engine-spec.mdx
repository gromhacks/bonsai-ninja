---
title: "Taint Engine Specification"
description: "The standalone taint engine contract, propagation semantics, and core concepts."
---

# Taint engine specification

The bonsai taint engine is a semantic data-flow analyzer that tracks
untrusted data through:

- intra-procedural CFG dataflow within a single function,
- inter-procedural call propagation across functions, and
- cross-file resolution via imports / exports / aliases,
- one IDG value-flow closure for every interprocedural consumer.

This doc covers the engine's contract, the ten concepts it implements, and
the read/write rules that consumers should understand. It's the engineering
reference. For the hands-on rule-authoring view, see [Pattern Guide](../pattern-guide.mdx).

## Contract

The reusable summary engine is exposed as a pure function:

```rust
interprocedural_taint(
    entry_func: FuncId,
    entry_sources: TokenSet,
    config: &InterTaintConfig,
    db: &AnalyzerDb,
) -> InterTaintResult
```

Given a non-empty `entry_sources`, it traces every reachable propagation.
Given an empty `entry_sources`, it produces zero records; the engine never
invents taint. Diagnostic and compatibility consumers can drive it through
`interprocedural_taint_with_caches` in `crates/taint/src/idg_api.rs`. That
façade composes concrete IDG seed nodes and runs one forward closure to
completion. `InterTaintCaches` remains as a source-compatible handle; graph
services are cached on `AnalyzerDb` by the canonical transfer-option
fingerprint.

`security taint-analysis`, `security source-analysis`, `security
sink-analysis`, `dump-taint`, inspect, and the public compatibility API use
the IDG-backed graph builders in
`crates/taint/src/reachable.rs`. Those builders resolve source seeds to IDG
nodes, apply configured transfer summaries, compute a precision-filtered
closure, and then cut/report only the relevant semantic target or lineage
corridor. This is the deployed source-to-sink path; direct raw-AST taint and
whole-workspace per-source/per-sink traversal are not production evidence
paths.

The IDG frontend consumes the same content-addressed compiler objects used by
callgraph, retrieval, linkage, inspect, and export. Persistence transfer
lowering runs once per function. It writes the canonical segment plus a typed
stitch record/node remap, then replays that record one segment at a time.
There is no second AST lowering pass and no alternate taint IR. Disk spilling
and memory-weighted scheduling change lifetimes/concurrency only; the closure
still processes every exact edge to its fixed point. Reached relations use
sorted external runs; pending LIFO frontiers append chunks to one temporary
file and truncate each consumed tail, so lowering the memory budget does not
multiply open file descriptors.

Return summaries use exact structural call boundaries rather than callee-name
search. Each function-local CSR is reconstructed from the canonical segment,
composed with current callee summaries, and discarded after compilation.
Callers are requeued whenever a callee summary grows, so recursive and mutually
recursive components converge to their least fixed point without an iteration
or call-depth cap. Context-sensitive queries retain sparse sorted call/return
rows and stream ordinary local edges into CSR instead of duplicating the full
workspace relation.

Projected symbolic facts are admitted by compiler demand, not by guessing
fields from a carrier name. The runtime persists the exact access-path keys
that occur in adapter-lowered projections and computes their backwards demand
through symbolic transforms. A bare aggregate read becomes wildcard demand
only when lowering emitted the non-traversable
`IntraAggregateConsume` marker for a real whole-value consumer. This marker is
metadata for demand compilation, never a scalar taint edge. Resolver-proven
local call arguments are excluded from wildcard consumption because the
callee's exact parameter projections provide the demand instead. Demand sets
and wildcard bases use the same lazy sparse/spill representations as closure;
no base-by-field matrix is materialized and no sibling projection is invented.

Backward target relevance is an optimization proof, not an alternate semantic
engine. Its result carries an explicit completeness bit. If an exact target
projection cannot be inverted through the persisted symbolic facts, relevance
becomes non-pruning for facts and nodes outside the proven set; the canonical
forward fixed point still decides reachability. An incomplete inverse may cost
work, but it may never reject a scalar seed, call boundary, or valid path.

Rulepack-free inspect taint rows use `Workspace::syntax_flow_graph` from
`crates/workspace/src/flow_query.rs`. That workspace facade is the only
command-facing switch between a warmed IDG target cut, an exact query-scoped
source/target IDG, and the canonical cached `EntryTaintGraph` compatibility
fallback. A warm query reuses the validated IDG accelerator; a cold query
builds only the complete compiler corridor required by its source and target
spans and never publishes that partial graph as the workspace-wide IDG. All
backends use the same adapter-lowered `FlowEvent`s, resolver-backed call
edges, precision ceiling, and taint crate propagation semantics.

Default entry seeds are centralized in `reachable.rs`:

- `default_entry_taint_seed` is used by fact-oriented and diagnostic
  consumers such as `dump-taint`.
- `default_entry_graph_seed` is used when materializing cached syntax-flow
  graphs so downstream filters can query a wider but still syntax-derived
  graph.

Do not duplicate the params/assignment-target/bare-call-argument seed logic
in command crates. Add new event container handling in the shared helper so
inspect, cached dataflow, and dump-taint stay aligned.

## Whole-program data-flow edge taxonomy

The language-neutral contract is
`bonsai_lang_api::taxonomy::FLOW_EDGE_TAXONOMY`. Adapters emit syntax facts
(`FlowEvent`, `Operation`, imports/exports, declarations, receiver facts);
the engine maps those facts onto shared IDG, resolver, rulepack, and control
facts. Audit classes are:

- `PASS` - implemented and backed by real tests where the language has the syntax.
- `ENGINE_ONLY` - shared engine/resolver/rulepack/control behavior, not a per-adapter syntax obligation.
- `STATIC_LIMIT` - represented conservatively, but precise static propagation needs runtime/config/framework facts.

Language-specific `N/A` belongs in the taint matrix, not this global table.

| Edge kind | Audit class | Primary carrier |
| --- | --- | --- |
| `LOCAL_ASSIGN` | `PASS` | `FlowEvent::Assign`, `IdgEdgeKind::IntraAssign` |
| `EXPR_PROPAGATION` | `PASS` | assignment/call/return operands, interpolation operands |
| `DEF_USE` | `PASS` | `TransferCtx::last_writer`, `IdgEdgeKind::IntraRead` |
| `ARG_TO_PARAM` | `PASS` | `Place::CallArg` to `Place::Param`, `IdgEdgeKind::InterCallArg` |
| `RECEIVER_TO_THIS` | `PASS` | call receiver facts, receiver param index, inter-call stitching |
| `RETURN_TO_CALLER` | `PASS` | `Place::Return` to `Place::CallRet`, `IdgEdgeKind::InterReturn` |
| `FIELD_WRITE` | `PASS` | field-sensitive writes, receiver field writes |
| `FIELD_READ` | `PASS` | field-sensitive reads, `IdgEdgeKind::IntraFieldRead` |
| `INDEX_WRITE` | `PASS` | normalized subscript writes |
| `INDEX_READ` | `PASS` | normalized subscript reads |
| `OBJECT_CONSTRUCTION` | `PASS` | constructor calls and receiver field writes |
| `DESTRUCTURING` | `PASS` | pattern/unpacking bindings from RHS operands |
| `CLOSURE_CAPTURE` | `STATIC_LIMIT` | syntactic closure/callback capture facts; dynamic closure factories stay conservative |
| `GLOBAL_ACCESS` | `PASS` | module/global places and bridge reads |
| `IMPORT_EXPORT` | `PASS` | `ImportSpec`, aliases, module exports, resolved callgraph facts |
| `ALIAS` | `PASS` | assignment aliases and import aliases |
| `DEREFERENCE` | `PASS` | dereference/address/borrow operations and normalized places |
| `HEAP_STORE` | `PASS` | field/constructor/container writes to allocated state |
| `HEAP_LOAD` | `PASS` | field/return projections from allocated state |
| `CONTAINER_STORE` | `PASS` | append/initializer/content writes |
| `CONTAINER_LOAD` | `PASS` | subscript/content reads |
| `ITERATION` | `PASS` | `LoopKind::ForEach`, generator/iterator carriers |
| `YIELD` | `PASS` | `FlowEvent::Yield`, `IdgEdgeKind::IntraYield` |
| `THROW_TO_CATCH` | `PASS` | intra-function `FlowEvent::Throw`/`Try`, `IdgEdgeKind::IntraThrow` |
| `AWAIT_RESOLUTION` | `PASS` | `FlowEvent::Await`, `IdgEdgeKind::IntraAwait` |
| `CALLBACK_INVOCATION` | `PASS` | resolved callback bindings and source callback specs |
| `EVENT_DISPATCH` | `ENGINE_ONLY` | framework/message source callback specs |
| `DYNAMIC_PROPERTY_ACCESS` | `STATIC_LIMIT` | normalized dynamic member/index facts, static best effort |
| `SERIALIZE` | `ENGINE_ONLY` | configured passthrough/source/sink semantics |
| `DESERIALIZE` | `ENGINE_ONLY` | configured passthrough/source/sink semantics |
| `SANITIZE` | `ENGINE_ONLY` | sanitizer rules and sanitizer credit |
| `SINK` | `ENGINE_ONLY` | sink rules and finding construction |
| `CONTROL_DEPENDENCE` | `ENGINE_ONLY` | branch conditions and CFG control facts |
| `IMPLICIT_FLOW` | `STATIC_LIMIT` | branch-condition influence is reported as a control fact, not default taint |
| `INTER_FILE` | `PASS` | cross-file IDG edges, imports/exports, resolved callgraph facts |
| `INTER_PACKAGE` | `ENGINE_ONLY` | dependency/package metadata and resolver facts |

The IDG edge enum stays intentionally compact. One `IdgEdgeKind` can carry
several taxonomy families; the precise construct remains available from the
originating syntax fact, place shape, resolver edge, or rulepack rule. The
edge-kind-to-taxonomy mapping is pinned by `crates/idg/src/edge_tests.rs`.

Static-analysis limits are explicit:

- `IdgEdgeKind::InterThrow` is reserved for future caller-context
  cross-function throw-to-catch stitching. Current exception propagation is
  native inside one function via `FlowEvent::Throw`/`Try` and
  `IdgEdgeKind::IntraThrow`; interprocedural exception evidence should not be
  claimed from the IDG until an `InterThrow` producer exists.
- Wildcard import (`X_07`, for example `from x import *`,
  `import static x.*`, `use x::*`, or `using namespace x`) is covered for every
  adapter that emits an unqualified wildcard `ImportSpec`. Namespace-qualified
  forms such as JavaScript / TypeScript `import * as ns` stay under `X_06`
  because the call site remains `ns.member(...)`, not a bare imported symbol.

The workspace exposes the canonical `InterTaintCaches` singleton via
`Workspace::inter_taint_caches()`. Production and diagnostic drivers borrow
that one instance so resolver answers per `(caller_func, call_span)`, file
alias maps, function summaries, and local bindings accumulate across runs in a
single CLI process. `Workspace::ingest_dir` calls `InterTaintCaches::clear()`
when a file edit lands.

Security source sites and source-returning helpers are resolved to concrete
IDG seed nodes before closure. A function that reads untrusted data and
returns only a status does not acquire a synthetic return edge; consumers
drive the engine with explicit source identities rather than a helper-name
inventory.

## Ten concepts

Each concept is pinned by a test in
`crates/taint/tests/ten_concept_alignment.rs`.

### 1. Source model

Caller-supplied seeds. `TokenSet` is `AHashSet<String>`. Identifiers can be
bare (`req`) or qualified (`obj.field`). Qualified identifiers track
per-field taint.

```rust
let seed: TokenSet = ["req".into()].into_iter().collect();
let result = interprocedural_taint(entry, &seed, &config, &db);
```

### 2. Sink model

The engine reports unresolved external calls in
`InterTaintResult.tainted_calls`. The security layer decides what's
actually a sink — the engine never filters by name. `TaintedCall.kind`
identifies the record shape. `kind: Call` carries syntactic call-argument
indices. `kind: Write` carries a synthetic operand for the assignment RHS
so the security matcher can enforce `arg_tainted: { index: 0 }` on write
rules. `kind: Return` is return-expression evidence and has no call
argument slots.

### 3. Sanitizer model

`InterTaintConfig.sanitizers: TokenSet` is accepted but doesn't change
propagation semantics. Sanitizer attribution is a security-layer concern;
the engine preserves the semantic taint path and leaves sanitizer status to
the security layer.

### 4. Call graph

Function A → function B edges, resolved via
`bonsai_resolve::resolve_callable_with_context`. Every edge carries a
`Precision` and an `EdgeKind` (Direct / Virtual / Indirect / Unknown).
Callable values pass through `bonsai_common::callable_reference_variants`
so the various forms (`&fn/1`, `fun fn/1`, `\&fn`, string callables, Ruby
method symbols, qualified tails) collapse to the same bare callable.

Adapters lower callback invocation syntax to real `Call` events before
this step. Perl `$cb->($value)` becomes a call named `$cb` with `$value`
as arg 0, so a prior `run_cb(\&executor, t)` binding can dispatch to
`executor(t)` without text fallbacks.

Receiver-callback shorthands (`cb.call`, `cb.run`, `cb.apply`) only
dispatch through local callable bindings when the binding resolves to a
function or method value. Constructed objects with same-named methods go
through receiver-type dispatch instead. Don't add results of
constructor-shaped calls (`repo = Repository.wrap(raw)`) to the callable
binding map.

Receiver-type facts are produced from adapter-emitted type aliases,
constructor syntax, and AST-derived `Decl.parent` ownership. The DB/index
path enriches `FlowEvent::Call::receiver_types` before callgraph, taint,
security, inspect, browse, and export consume the facts. The global pass
extends direct receiver types through indexed base classes so every
consumer sees the same `Child` plus `Base` receiver context. Ambiguous
type names fail closed; the engine must not merge unrelated class
hierarchies by bare name.

When the receiver is `self.<field>` / `this.<field>`, the resolver
also consults the enclosing class's constructor `receiver_field_writes`
and reads the source-parameter type from the constructor's
`type_aliases`. `def __init__(self, runner: CommandRunner): self.runner
= runner` makes every other method's `self.runner.X(...)` call
dispatch through `CommandRunner.X` — without the engine having to
re-walk `__init__`'s body at every receiver call.

### 5. Data-flow graph

Per-function `FlowEvent::Assign` events carry `target`, `source_name`,
`source_call`, `source_call_args`, and `source_names` so taint flows
through compound RHS without re-parsing. `source_names` preserves
qualified RHS reads (`obj.field`, `obj['field']`, `obj->field`) as
value-bearing operands, including grammars that split selectors into
multiple nodes.

Call-site value shape is likewise compiler-owned. Each adapter lowers exact
scalar literals, exact named aggregate fields from structurally complete
spread-free aggregates, complete positional
sequences, addressable places, and nested expression carriers into
`CallArgumentValueFact`. When the complete argument is an inline callback,
the same fact retains its Tree-sitter-lowered parameter bindings in source
order. Unknown sequence elements remain explicit `None`
slots, so a rule can prove `['sh', '-c', dynamic]` without guessing the
dynamic value. Match constraints such as `arg_sequence_items_equal` compare
those typed facts and fail closed when the adapter did not prove the shape;
they never parse `CallArg::value_text`.

Named aggregate proofs track each path independently. A dynamic value in an
unrelated sibling does not erase an exact boolean/string field, but a spread,
computed key, duplicate path, or unknown aggregate shape rejects the proof.
This permits exact configuration checks such as `followRedirect: false`
without pretending a dynamic timeout is constant.

Configured character-transform facts are also compiler-owned and
security-neutral. An adapter can lower the exact decoded mapping sequence,
factory identity, operation identity, input, output, and transform span. The
rulepack selects accepted providers and the complete security-required mapping
inventory; shared guard analysis only compares the typed records. Extra,
missing, duplicate, or mismatched mappings fail closed. Neither adapter nor
engine contains a security API-name or escape-table inventory.

Value-preserving call results are explicit rulepack models, not text
fallbacks or engine API tables. When a sanitizer/passthrough rule declares
`taint_semantics.call_result_passthrough_args` or
`call_result_passthrough_receiver`, string/list transforms such as
`upper`, `toUpperCase`, `strings.ToUpper`, `TrimSpace`, and
formatting/join helpers bridge their configured input operands to the
assignment result. Go `append` bridges each appended operand to the
returned slice while the container-mutator rule below separately models
receiver descendant state.

Constructor / object-initializer assignments with named fields are
field-sensitive. `env = Envelope({cmd: raw, kind: K})` seeds `env.cmd` from
`raw` without tainting bare `env` or `env.kind`. Positional constructors
fall back to descendant-container taint only when a type-like call
actually receives tainted data.

Indexed buffer bookkeeping does not overwrite the whole container. Adapter
shapes like `buf[sizeof(buf) - 1] = '\0'` may emit both precise indexed
writes and structural base tokens; the IDG drops the `sizeof` metadata and
same-span base self-write so the terminator assignment cannot erase the
tainted buffer value.

### 6. Module resolver

`bonsai_resolve::alias_map_for_file` builds `{local_name → AliasTarget}`
from `ImportSpec` entries. The engine consults the alias map at every call
site to translate local names to global decls.

### 7. Context sensitivity

Same function, different seeds → distinct results. The engine memoises by
`FunctionSeed { func, seed }` (sorted) so each `(func, seed)` pair has its
own analysis. Recursive SCCs converge because transfer is monotone over a
finite interned-place lattice; there is no recursion-depth or iteration cap.

### 8. Field sensitivity

`obj.value = tainted` writes `obj.value` into state, not bare `obj`. Reads
of `obj.other` don't match. The carrier-arg case (`process(obj)` with
state `{obj.value}`) still propagates via
`state_qualified_token_matches_text`. A bare tainted lifecycle carrier
(`helper(obj)`) doesn't taint independent fields or locals derived only
from those fields.

### 9. Path sensitivity

Branches don't dilute clean paths. `Branch` handling clones state per arm,
walks both, and unions post-states. Clean reassignment in both branches
clears prior taint at the merge.

The adapter contract requires both arms to be present. The kit's
`branch_repair` recovers the partition for grammars that lump both arms
together. That repair runs before taint analysis, so inspect, trace,
export, and security all see the same branch shape.

Branch proofs consume adapter-lowered `ConditionExpressionFact` trees and
typed `ConditionOperandFact` scalar values. The owning adapter maps its
grammar's boolean, equality, membership, type-test, and runtime-truth nodes;
`ConditionExpressionFact::Truthy` preserves the tested operand rather than a
rendered token. Falsey fallbacks retain their dynamic projection. Shared
analysis does not maintain a language-token/operator inventory. A rendered
condition string remains diagnostic context only.

**Pattern arms.** Languages where `match`/`switch` parses as a flat
`Branch` with arms concatenated into `then_events` (Scala, Swift, Rust)
get peeled by `split_match_arms_in_branch_events`. Each arm becomes its
own nested `Branch::then_events`.

**Typed catch arms.** `Try` events carry `catch_types` populated by
adapter post-processes. The engine seeds the catch param only when at
least one body throw is type-assignable:

- Empty `catch_types` (catch-all) → conservative seed.
- Catch type is a known absolute root (`Exception`, `Throwable`, `Error`,
  `System.Exception`, `Object`) → conservative seed.
- Otherwise → exact-name match between `Throw::thrown_type` and
  `catch_types`. Compound throws fall back to conservative.
- `RuntimeException` is intentionally NOT a root — it doesn't catch
  checked exceptions.

**Async and deferred bodies.** Tree-sitter-owned async bodies remain visible
to the engine regardless of the runtime API that receives them. Shared code
does not recognize executor names. `FlowEvent::Defer` bodies are observed so
dangerous sinks still surface, while their state stays outside the caller's
sequential continuation; an adapter may emit that event only from syntax that
proves a deferred scope. Runtime-specific detachment needs an explicit typed
summary capability and must not be guessed from call text.

### 10. Summary generation

`FunctionSummary` records positional taint effects:

- `returns_taint_of`: parameter indices whose value taint reaches the
  return.
- `returns_descendant_taint_of`: indices whose explicit descendant taint
  reaches the return.
- `returns_container_taint_of`: indices whose direct value gets embedded
  into a returned container, so the caller's assignment target carries
  descendant taint.
- `returns_field_taint_of`: exact returned-container fields whose values
  come from a parameter. `return {"user": user}` taints `result.user`,
  not `result.*`.
- `returns_element_taint_of`: exact returned tuple/list positions whose
  values come from a parameter. `return {"ok", p}` / `return (clean, p)`
  taints only the second destructured binding in `{a, b} = helper(p)` or
  `(a, b) = helper(p)`.
- `taints_params_from`: parameter-to-parameter side effects (e.g.
  `join(dst, ..., src)` writes `src` into `dst`).

Caller's `apply_return_taint` consults the summary when processing
`var = callee(args)`. Adapters set `Decl.has_implicit_returns` only for
language or body forms where a final expression really returns a value
(Rust/Ruby tail expressions, expression-bodied lambdas/functions). The
summary pass only uses terminal-tail evidence when that adapter fact is
present; ordinary terminal assignments are not treated as returns.

When an adapter emits `Assign.source_call` without `source_call_args` but
also emits the sibling `FlowEvent::Call`, IDG lowering correlates the two
AST-derived facts and uses the call's argument list. This is not a text
fallback; return-summary checks still use positional arguments.

Container summaries are intentionally split. Whole-container taint is a
fallback for dynamic aggregate returns. Named-field returns and positional
tuple/list returns are applied only when the caller assignment exposes the
matching field or destructuring index; they must not scalar-taint sibling
fields or sibling bindings.

## Workspace-level result caches

The engine's per-call results are cached at the workspace layer so a
second query of the same shape against the same workspace becomes a
lookup instead of a fresh propagation pass.

- **`DataFlowCache`** (`crates/workspace/src/dataflow.rs`) — per-`FuncId`
  `KindedTokens` + `EntryTaintGraph`. Loaded from a valid persisted
  sidecar when present, computed on demand for exact query scopes, and
  fully rebuilt by explicit full-prewarm SDK opens or by the compatibility
  dataflow-only path, `index --prewarm-dataflow`; persisted to
  external-cache `dataflow.v3.factstore` (the older `dataflow.v2.bin` is still
  readable for warm reopens). The cache carries each entry's transitive
  dependency file set, so an edit to any callee invalidates only the
  affected entries.
- **`ValueFlowCache`** (`crates/workspace/src/value_flow.rs`) — per-`FuncId`
  compatibility `ValueFlowGraph` projected from the canonical IDG on demand
  and cached for reuse. It is not eagerly materialized for every callable,
  because that would repeat a workspace closure per function. Each requested
  entry also ships a
  `returning_seed_names: AHashSet<String>` precomputed at graph-build
  time — the set of param/assign-target names whose forward closure
  reaches a `Return` node in the same function. Compatibility callers can use
  that summary without another closure. Explicit compatibility prewarm/query
  paths may persist it to external-cache `value_flow.v*.factstore`; security and
  native export use canonical IDG summaries instead.
- **`TaintGraphIndex`** (`crates/workspace/src/taint_index.rs`) —
  `(source_func, sorted_seed_key) → Arc<EntryTaintGraph>`. Lifted
  out of the per-invocation map in `build_findings_chain_aware` so
  a second `taint-analysis` against the same workspace + rulepack
  reuses the cached graph. Persisted to
  external-cache `taint_graph.v*.<mode>.<config>.factstore` with version +
  config-fingerprint guard; mismatched fingerprints invalidate and distinct
  source/taint analysis configurations do not overwrite each other.
  Write-through persistence is
  default-on and can be disabled with `BONSAI_TAINT_GRAPH_PERSIST=0`.
  `security taint-analysis`, `security source-analysis`, and `security
  sink-analysis` publish the
  prepare/finish decision through `AnalysisProgress::Note` so callers can
  see scope, memory/disk hit or miss, config invalidation, and
  write-through status.
- **`Workspace::cached_resolved_call_graph()`** — singleton
  `Arc<ResolvedCallGraph>` shared across `inspect`, `security`,
  `dump-callgraph`, the `DataFlowCache`, and the `FlowIdCache`. The
  data caches receive it via `seed_call_graph(arc)` when a query or
  explicit prewarm path has built it so they don't rebuild identical
  content. Security's serial planner passes its exact graph by immutable
  reference into parallel finding and compiler-guard attribution. A Rayon
  worker must not initialize this lazy workspace cache: doing so can
  recursively acquire compiler-header state and deadlock the worker pool.

All caches are `parking_lot::RwLock`-backed and safe to share
across rayon worker threads. `Workspace::ingest_dir` clears or
invalidates each one when a file edit lands.

## Resolver semantics and precision

`resolve_callable_with_context` (`crates/resolve/src/lib.rs`)
is the single semantic entry point. It applies, in order:

1. `Visibility::Private` filter — keep only same-file decls.
2. `Visibility::Module` — exact module/package match.
3. `Visibility::ModuleTree` — declaring module plus lexical descendants
   (Rust's default and `pub(self)` scope).
4. `Visibility::Crate` — top-segment match against the caller's
   `ModulePath`.
5. Receiver-type narrowing when `ctx.receiver_type` is `Some` —
   `Decl.parent` must be the receiver type or in its base chain.

Unqualified same-directory top-level fallbacks are enabled only when the
caller adapter declares `LanguageCapabilities::same_directory_unqualified_calls`.
The caller must also provide the workspace file-path lookup in
`ResolveContext`; otherwise the fallback is unavailable. File-scoped module
languages leave the capability false and must import or alias the symbol, so
the resolver fails closed instead of fabricating a cross-file edge.

The engine's `resolve_call_candidates_with_caller_at` builds the
`ResolveContext` from each call site's `(caller_func, call_span)`,
threads it into the resolver, and memoises the answer in
`InterTaintCaches::resolved_calls_by_site`. When the answer is a
single candidate, the edge is `EdgeKind::Direct` /
`Precision::Narrowed`. Multiple candidates are emitted only when adapter
facts semantically explain the set, such as typed virtual dispatch over a
known receiver family or a same-scope overload family. Overload families are
then narrowed by declared argument/parameter type facts when available, so a
known `String` actual does not propagate into an `Int` overload. The remaining
multi-candidate edges are `EdgeKind::Virtual` /
`Precision::Narrowed`. Contextless or unexplained multi-candidate sets are
not public evidence and must be surfaced as incompleteness/debug metadata,
not widened into `Precision::OverApproximate` findings. There is no
"used-tail-fallback widens single candidates to OverApproximate"
exception — `ResolveContext` filtering already narrowed the result.

Receiver-type-unknown calls produce no semantic call target. Debug-only
helpers may explain why a name looked dynamic, but public review/security
flows must wait for receiver-type, alias, module, or model facts before
emitting an edge.

Closure-factory calls are resolved only when the factory's body returns a
single lambda/function literal in the same file. The callgraph binds the
assignment target to that returned callable; ambiguous or multi-return
factories stay unresolved rather than tainting every later indirect call.

## Engine layers

The taint stack is layered:

- **Layer 1 — compiler facts** (language adapters, index, resolver, and
  callgraph): CST/AST nodes become typed declarations, calls, arguments,
  assignments, indexed writes, imports, receiver types, and semantic call
  edges. Syntax-only operations use typed `CallKind` values; shared lowering
  never invents a provider pseudo-callee.
- **Layer 2 — canonical dataflow** (`crates/idg`): `builder.rs`,
  `transfer.rs`, and `workspace_adapter.rs` lower those facts into one graph
  containing intra-function, cross-call, return, field, capture, callback,
  write-back, and inheritance edges.
- **Layer 3 — seed/query conversion** (`reachable.rs`): composes canonical
  IDG seed nodes and converts closures back into public call/argument evidence
  for security, inspect, dump-taint, and compatibility callers.
- **Layer 4 — compatibility surfaces** (`assignment.rs`, `intra.rs`, and
  `idg_api.rs`): preserves local helper and public result types while
  interprocedural answers come exclusively from the IDG. These projections do
  not implement a second interprocedural engine.

Native JSON export follows the same boundary. `taint_graph.assign_chains` is a
per-parameter, function-local projection of the IDG closure. The stable
`taint_graph.intra_taint` block table remains a local CFG presentation until IDG
points carry `BasicBlockId`; every row therefore serializes
`backend: "cfg_local"`. It is compatibility/debug data, not a second
interprocedural engine and not an alternate source-to-sink result.

`InterTaintConfig` contains semantic transfer inputs only:

- `clean_output_overwrites: Vec<CleanOutputOverwrite>` — declarative
  overwrite shapes derived from rulepack `taint_semantics`.
- `source_output_args: Vec<SourceOutputArgs>` — source calls whose
  listed output arguments receive taint, such as `recv`, `SSL_read`,
  and `fgets` buffers. These are derived from source rules, not from a
  built-in API list.
- `source_callback_args: Vec<SourceCallbackArgs>` — rule-declared source
  callbacks and parameter positions. Named callbacks resolve through exact
  AST/callgraph identities. Direct call-argument lambdas use the adapter's
  `CallArgumentValueFact::inline_callback_params` and their already-inlined
  body events; the delivered parameter retains whole-value provenance so an
  exact projection such as `input.column` inherits the source without
  widening to sibling fields. Neither path parses callback text or carries a
  framework/API-name inventory in the engine.
- `call_result_passthroughs: Vec<CallResultPassthrough>` — rulepack
  declarations that selected call arguments or the method receiver flow
  to the call result, such as URL decode/unescape helpers. The engine
  owns the transfer primitive, not a hardcoded API list.
- `output_arg_flows: Vec<OutputArgFlow>` — rulepack declarations that
  selected value arguments flow into an output argument. This covers
  formatter/copy-style APIs without putting library names or argument
  orders in the engine.
- `receiver_state_propagations: Vec<ReceiverStatePropagation>` —
  rulepack-declared receiver/method shapes that move argument taint into
  receiver state for later receiver-only sinks. The engine owns no global
  mutator-name list.
- `max_edge_precision: Option<Precision>` — the accepted evidence class;
  production consumers use exact/narrowed semantic edges and reject guessed
  over-approximate edges.

Language syntax and ABI intrinsics remain in adapter/compiler lowering (for
example C variadic builtins). Library/framework API behavior belongs only in
rulepack/config transfer semantics. Package-gated rules require adapter import
facts, resolved workspace-import facts, or dependency-manifest evidence;
directory names and conventional parameter spellings do not manufacture a
framework identity.

Sanitizer attachment follows the same boundary. Generic lineage policies may
prove that a hardened factory constructs the eventual sink receiver, but the
builder/factory targets are `RuleTarget` values inherited from rulepack
metadata (`receiver_factory_lineage_builders`). Shared analysis compares only
typed calls against those targets and contains no constructor-name list.

`InterTaintResult` returns:

- `per_function: AHashMap<FunctionSeed, IntraTaintResult>` — per-pair
  caches.
- `call_records: Vec<CallPropagation>` — every cross-function flow.
- `tainted_calls: Vec<TaintedCall>` — every call/write/return whose
  operands carry tainted values.
- `precision: Precision` — worst seen along any traversed edge.
- `pairs_analyzed: u32` — number of graph pairs represented by the closure.

The closure kernel is a sparse monotone stack over numeric CSR edges. It has
no breadth-first name search, depth bound, iteration limit, or result cap and
runs until the reachable fixed point is exhausted. Hop/path budgets exist only
on explicit neighbourhood and path-rendering diagnostics; they never feed the
semantic taint result.

## Read-side semantic checks

Read propagation is graph-based. Adapters lower identifier, projection,
interpolation, destructuring, call-argument, and passing-mode facts from the
AST into `FlowEvent`s. IDG transfer interns those facts as `Place::Read`,
`Place::Write`, `CallArg`, and `CallRet` nodes, then follows exact or narrowed
edges. Engines do not recover constructor names, mutable-reference keywords,
callback factories, or nested calls from raw expression text.

Caller-visible out/ref/pointer write-back follows the same boundary. The
adapter classifies the argument's passing mode and, when the grammar wraps the
addressable place in an indirect expression, returns that exact operand node.
Shared lowering recursively normalizes the proven place and the IDG stitches
the callee parameter's outbound writes back to it. No shared operator or
language token table participates.

Qualified-place canonicalization remains shared for compatibility seed names
(`obj['k']` ↔ `obj.k`, language sigils, and wildcard descendants), but it does
not invent a read that the adapter failed to emit. This is what keeps
`data.other` from inheriting `data.value` while still allowing an explicit
whole-aggregate destructuring edge.

At resolved call boundaries, a projected caller argument maps to the matching
callee projection. For a method whose grammar has no explicit receiver
parameter, `carrier.field` maps to `this.field`; the scalar `carrier` maps only
to `this`. Constructor or method stitching must never collapse the scalar
carrier into a projected field merely because that field is demanded later.
Expression-bodied properties and other accessor sugar follow the same rule:
the owning adapter emits the exact projection place and call-site fact, and
shared IDG code joins those facts by span.

Some Tree-sitter grammars represent `container.value.field` as a receiver
place (`container.value`) plus a resolved selector accessor (`this.field`).
The sparse IDG demand planner composes those two typed facts into the finite
suffix `value.field` before field forwarding. This is not token guessing: both
halves must exist in adapter-lowered IR and the selector call must resolve.
The composed demand lets exact state survive constructor, inheritance, and
computed-property hops without admitting an unobserved sibling or recursive
field.

A root binding that receives a complete call result may feed a later,
adapter-emitted projection of that value (`header = reader.Next()` followed by
`header.Name`). An exact writer for the projected field wins, including a clean
overwrite. A call result assigned into an existing projection is not promoted
to a new whole-object root; it remains governed by the finite AST projection
closure so sibling and recursive fields are not invented.

## Branch conditions and constant facts

`FlowEvent::Branch` optionally carries condition text for diagnostics and
rendering only. Semantic consumers do not reparse that string. Each owning
language adapter emits `BranchConditionFact` with an AST-derived polarity and,
where supported, a typed `ConditionExpressionFact` tree containing exact
boolean, equality, membership, type-test, runtime-truth, and scalar-literal
facts. Security
and control-flow proofs relate calls to the exact condition span and consume
that compiler IR. Native export includes the same facts.

Tainted conditions are not dataflow. A tainted branch guard may choose
which clean/dynamic container is returned, but it does not make every
field in that returned container tainted. Returned containers are tainted
only by explicit data movement into the returned value or one of its
tracked fields or positional elements.

## Append-style mutators and lifecycle calls

Append-style container mutators (`push`, `append`, `add`, `addAll`, queue
enqueue forms) propagate tainted arguments into the receiver's descendant
state (`items.*`), not into the receiver scalar value. This models
array/list contents without treating unrelated lifecycle fields as
tainted.

Carrier passing where a specific field is tainted (`client.argv`,
`env.cmd`) gives the callee matching field state (`param.argv`,
`param.cmd`). The carrier parameter itself isn't marked as a tainted
scalar unless the caller had explicit value taint — so `free(client)`
doesn't taint every field reachable below `client`.

## Drift gates

Engine semantics are pinned by snapshots under `.snapshots/`:

- `COVERAGE_BASELINE.snapshot` — per-language capability matrix.
- `ADAPTER_CAPABILITIES.snapshot` — adapter-claimed coverage.

`scripts/audit-adapter-flow-events.sh --check` complements those declarations
with behavioral conformance. It parses canonical valid programs through every
adapter and inspects the emitted `FlowEvent` trees, including async/yield
shapes. It intentionally does not count enum spellings in source: shared
walkers inspect events too, and extracting common lowering code must not look
like a semantic coverage loss. CI fails on either declared-capability drift or
behavioral fact drift.
